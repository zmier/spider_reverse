# Root Python environment for spiders/
#
# Install from the repository root:
#   python -m venv .venv
#   .venv/bin/python -m pip install -r requirements.txt

# Core HTTP / HTML parsing
requests==2.31.0
beautifulsoup4==4.12.2
lxml>=5.0.0
python-dotenv>=1.0.0

# JavaScript reverse-engineering helpers
PyExecJS==1.5.1

# Crypto / encoding helpers used by demo cases
pycryptodome==3.18.0
pkcs7==0.1.2

# CAPTCHA / image helpers used by demo cases
# Pillow 10.0.0 does not build cleanly on Python 3.13.
Pillow>=10.4.0
# Older demo pin 1.4.8 only supports Python < 3.11.
ddddocr>=1.6.0

# Browser / network fallback tools used by selected cases
DrissionPage>=4.0.0
curl_cffi>=0.6.0
websockets>=12.0

# Project-level utilities
python-docx>=1.1.0
paramiko>=3.4.0
